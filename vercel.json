{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "routes": [
    {
      "src": "/server/(.*)",
      "dest": "/server/$1"
    },
    {
      "src": "/client/(.*)",
      "dest": "client/index.html"
    }
  ]
}
